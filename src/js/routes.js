/**
 * This contains page routes for the entire application.
 * @module [{js}routes]
 */
export const paths = {
  // Core pages.
  home: '/',
};

export default [{
  path: paths.home,
  exact: true,
  title: 'Home',
}];
